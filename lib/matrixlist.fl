//core matrix arithmetic library
//implements matrix as a list of lists

let zeros n =
    replicate n 0
;

letrec sum [] = '0
    /\sum vs =
    (hd vs) + (sum (tl vs))
;

letrec add [] [] = []
    /\ add us vs =
    ((hd us) + (hd vs)) : (add (tl us) (tl vs))
;
